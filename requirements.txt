# Audio processing libraries
# Install with: pip install -r requirements.txt

# Option 1: pydub (recommended - supports both WAV and MP3)
pydub>=0.25.1

# Option 2: librosa + soundfile (more control, supports WAV and MP3)
# Uncomment if you prefer this method:
# librosa>=0.10.0
# soundfile>=0.12.1
# numpy>=1.24.0

# Note: Both options require ffmpeg to be installed on your system for MP3 support
# Install ffmpeg:
#   - Ubuntu/Debian: sudo apt-get install ffmpeg
#   - macOS: brew install ffmpeg
#   - Windows: download from https://ffmpeg.org/

# Supported audio formats:
#   - WAV (both methods)
#   - MP3 (both methods, requires ffmpeg)
